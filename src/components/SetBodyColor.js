export function setBodyColor({color}) {
    document.documentElement.style.setProperty('--bodyColor', color)
}

export function setTextColor({color}) {
    document.documentElement.style.setProperty('--bodyText', color)
}